<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FMES.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FMES"
    Shell.FlyoutBehavior="Disabled"
    Title="FMES">

    <ShellContent
        Title="　"
        ContentTemplate="{DataTemplate local:MainPage}"
        Icon="icon80x80.png"
        Route="MainPage" />
    <ShellContent
        Title="　"
        ContentTemplate="{DataTemplate local:configPage}"
        Icon="config80x80.png"
         Route="configPage" />

<ShellContent
        Title="　"
        ContentTemplate="{DataTemplate local:logoutPage}"
        Icon="config80x80.png"
         Route="lougoutPage" />
</Shell>
